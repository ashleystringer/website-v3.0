name: autofix.ci
on:
    pull_request:
    push:
permissions: 
    contents: write
jobs:
    prettier:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
            - run: |
                  npm run prettier
